数组笔记
索引可以有语义也可以没有语义
优点：可以快速查询。array[2]直接查询。
所以数组最好应用与索引有语义的情况，例如索引代表学生的学号数组中存放学生的成绩，可以根据
学号直接查询到成绩。但不是所有有语义的情况都使用数组，例如身份证号。

基于Java的数组封装一个动态数组

capacity:数组容量最多能装元素个数
size:数组实际装了多少个元素，初始为0，就是第一个空的可存放元素的位置

向数组尾添加元素:直接对数组size位置赋值，size是第一个空的可存放元素的位置

向数组特定位置添加元素:(index,e)将size-1位置元素复制到size的位置，将size-2位置元素复制
                     到size-1的位置，循环操作一直到index位置元素复制到index+1的位置，
                     然后用e直接覆盖原来index位置的元素，size++，size表示第一个空的可
                     存放元素的位置即null

向数组特定位置删除元素:(index,e)将index+1位置的元素赋值给index位置，index+2位置的元素赋
                     值给index+1位置，循环操作一直到size-1位置元素赋值给size-2位置，
                     size--