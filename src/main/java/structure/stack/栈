栈
1.栈也是一种线性结构
2.相比数组，栈对应的操作是数组的子集
3.只能从一端添加元素和删除元素，这一端成为栈顶
4.先进后出
5.相关应用：
  编辑器中undo操作
  系统栈

arraystack复杂度分析:
  由于底层是数组，push和pop的操作直接取最后一个索引位置的元素，所以复杂度是o(1)，如果发生
  了resize均摊下来还是o(1)
  void push(E)       o(1) 均摊
  E pop()            o(1) 均摊
  E peek()
  int getSize()
  boolean isEmpty()